{% extends 'base.html.twig' %}

{% block body %}

<div class="admin-main container">

    <div class="jumbotron">
    <h2 class="display-4">{{ "USER_EDIT"|trans }}</h2>
    <hr class="my-4">


            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
            <div class="button button__row-list button__right">
                <input class="btn btn-primary" type="submit" value="{{ "EDIT"|trans }}" />


                {{ form_end(edit_form) }}

                <a href="{{ path('user_index') }}" class="btn btn-dark">{{ "BACK_TO_THE_LIST"|trans }}</a>

            {{ form_start(delete_form) }}
                    <a href="" class="btn btn-danger" value="Delete">{{ "DELETE"|trans }}</a>

            {{ form_end(delete_form) }}
        </div>
    </div>
</div>
{% endblock %}
