{% extends 'base.html.twig' %}

{% block body %}


<div class="admin-main container">

    <div class="jumbotron">
        <h2 class="display-4">{{ "USER_CREATION"|trans }}</h2>
        <hr class="my-4">

        <div class="admin-main__form">

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="button button__column-list button__right">
            <input type="submit" class="btn btn-primary" value="Create" />
            {{ form_end(form) }}
                <a href="{{ path('user_index') }}" class="btn btn-dark">{{ "BACK_TO_THE_LIST"|trans }}</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
