{% extends 'base.html.twig' %}

{% block body %}

<div class="admin-main container">

    <div class="jumbotron">
        <h2 class="display-4">{{ "SEMESTER_CREATION"|trans }}</h2>
        <hr class="my-4">

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="button button__column-list button__right">
                <button class="btn btn-primary" type="submit">{{ "ADD"|trans }}</button>
                <a href="{{ path('semester_index') }}" class="btn btn-dark">{{ "BACK_TO_THE_LIST"|trans }}</a>
            </div>
        {{ form_end(form) }}

    </div>
</div>
{% endblock %}
