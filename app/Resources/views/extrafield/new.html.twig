{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ "EXTRAFIELD_CREATION"|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('extrafield_index') }}">{{ "BACK_TO_THE_LIST"|trans }}</a>
        </li>
    </ul>
{% endblock %}
